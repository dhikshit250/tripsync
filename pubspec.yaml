  name: tripsync
  description: "A new Flutter project."
  publish_to: 'none'

  version: 1.0.0+1

  environment:
    sdk: '>=3.0.0 <4.0.0' # Updated for broader compatibility

  dependencies:
    flutter:
      sdk: flutter

    # Firebase
    firebase_core: ^3.15.1
    firebase_auth: ^5.6.2
    cloud_firestore: ^5.6.11
    http: ^1.2.1

      # Google Sign In
    google_sign_in: ^6.1.4

    # UI/UX
    google_fonts: ^6.2.1 # Added for custom fonts
    cupertino_icons: ^1.0.8

  dev_dependencies:
    flutter_test:
      sdk: flutter
    flutter_launcher_icons: ^0.13.1

    flutter_lints: ^6.0.0

  flutter_icons:
    android: true
    ios: true
    image_path: "assets/images/Tripsync_logo.png"

  flutter:
    uses-material-design: true

    # Ensure you have created this folder structure
    assets:
      - assets/images/